#include "main.h"
/**
 * print_numbers - function that checks for uppercase character.
 * Return: 0
 */
void print_numbers(void)
{
	int y;

	for (y = 0; x <= 10; y++)
		_putchar(y + '0');
	_putchar('\n');
}
